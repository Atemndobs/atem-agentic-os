# Project Worktrees
Project handoff files live in:
`<repo>/.harness/`

Worktree handoff files live in:
`<repo>/.harness/handoffs/<TASK-ID>/`

Main checkout is integration authority. Feature implementation should happen in worktrees.
